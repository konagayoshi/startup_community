<% content_for(:title) do %>
  SEARCH
<% end %>

<%= form_for(@student, url:search_students_path,  html: { method: :get, class: 'form-horizontal', role: 'form' }) do |f| %>
  <%= render 'search_form', f: f %>
<% end %>
